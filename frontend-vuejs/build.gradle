task npmInstall(type: Exec) {
  executable 'npm'
  args = ['install']
}

task npmRunBuild(type: Exec) {
  executable 'npm'
  args = ['run', 'build']
}

task npmRunDebug(type: Exec) {
  executable 'npm'
  args = ['run', 'serve']
}
